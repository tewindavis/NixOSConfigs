-- Custom plugin specs go here. Empty for now — this file exists only so
-- lazy.nvim's `{ import = "plugins" }` in config/lazy.lua has something to
-- find (an empty directory makes that import fail at startup).
return {}
